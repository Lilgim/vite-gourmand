=== Init (2026-07-14 20:10) ===
Boucle Ralph initialisée. 12 stories dérivées de .agent-forge/PLAN.md et du brief ECF.
Environnement : Windows 11, Bun 1.3.6, Docker 29.2 + Compose v5 (PostgreSQL/MongoDB en conteneurs), Next.js 16.2.10.
Gitflow : une branche feature/<story> depuis dev, merge --no-ff dans dev après validation (lint + typecheck + build + tests).
Contrainte : dépôt public — aucun secret, aucun contenu Studi.

=== Iteration 1 (2026-07-14 20:35) ===
Story: story-db - Fondations données : Docker, scripts SQL, clients PG/Mongo
Status: PASSED
Validations réelles : 01_create.sql + 02_seed.sql sur base vide (COMMIT x2), check-db.ts (6 menus, 3 users, mongo ping 1), biome 10 fichiers OK, tsc OK, next build OK.
Learnings:
- Un PostgreSQL natif Windows occupe 5432 → conteneurs mappés 5433 (pg) / 27018 (mongo).
- Driver mongodb (bson) incompatible Bun sur Windows (node:v8 isBuildingSnapshot) → scripts utilitaires avec `node --env-file=.env` ; l'app Next tourne sur Node donc pas d'impact.
- Out-File PowerShell écrit un BOM UTF-8 qui corrompt la 1re clé d'un .env → écrire via [IO.File]::WriteAllText sans BOM.
- Premier bind d'un port docker parfois refusé juste après un up raté (relais WSL pas libéré) → retry suffit.
Files: docker-compose.yml, sql/01_create.sql, sql/02_seed.sql, src/lib/{env,db,mongo}.ts, scripts/check-db.ts, .env.example
Workflow: CodeRabbit installé par lilgim → chaque story part en PR feature→dev pour review indépendante avant merge.

=== Iteration 2 (2026-07-14 21:05) ===
Story: story-auth - Authentification et rôles
Status: PASSED
Validations réelles : lint/typecheck/build OK (proxy détecté), smoke test serveur prod — /, /connexion, /inscription en 200, /compte sans cookie → 307 /connexion.
Learnings:
- Next 16 : middleware.ts renommé proxy.ts (export function proxy) ; cookies()/params/searchParams async obligatoires.
- Pattern DAL du guide officiel : getCurrentUser mémoïsé avec React cache(), proxy = contrôle optimiste cookie uniquement, jamais de requête DB dans le proxy.
- Fichier "use server" : seuls les exports doivent être async ; les helpers non exportés peuvent être sync.
- CodeRabbit ne review pas les PR vers dev par défaut (base non-default) → .coderabbit.yaml avec base_branches [dev, main] + trigger manuel @coderabbitai review.
Files: src/lib/{auth,validation}.ts, src/app/actions/auth.ts, src/app/(auth)/**, src/app/compte/page.tsx, src/components/{header,form-field}.tsx, src/proxy.ts, .coderabbit.yaml
PRs: #1 (db-foundations), #2 (auth) — reviews CodeRabbit déclenchées, à traiter avant merge.

=== Iteration 3 (2026-07-14 21:30) ===
Story: story-public-pages - Pages publiques et layout
Status: PASSED
Validations réelles : lint/typecheck/build OK ; smoke test serveur prod — accueil affiche l'avis approuvé seedé et les horaires depuis PG, /entreprise /contact /mentions-legales /cgv en 200, règle des 600 € visible dans les CGV.
Learnings:
- Biome a11y strict : pas d'aria-label sur <p> → pattern aria-hidden pour les étoiles + span sr-only pour le texte lecteur d'écran.
- Le sujet demande de "consulter" contact → page coordonnées + horaires, pas de formulaire (scope minimal conforme).
Files: src/app/{page,entreprise/page,contact/page,mentions-legales/page,cgv/page}.tsx, src/components/footer.tsx, src/lib/queries/home.ts, src/app/layout.tsx

=== Iteration 4 (2026-07-14 22:00) ===
Story: story-menus - Catalogue des menus avec filtres
Status: PASSED
Validations réelles : lint/typecheck/build OK ; smoke test — /menus rend 6 menus + filtres, /menus/1 rend plats/allergènes/galerie, /menus/999 et /menus/abc → 404, SVG servis en 200.
Learnings:
- PIÈGE ENCODAGE : (Get-Content -Raw) -replace | Set-Content casse l'UTF-8 d'un fichier accentué (mojibake) → toujours utiliser l'outil Edit pour modifier des fichiers existants, git checkout pour restaurer.
- Filtres "sans rechargement" : composant client qui filtre en mémoire la liste passée par le Server Component — simple et défendable ; compteur de résultats en aria-live.
- Visuels menus : SVG placeholders locaux dans public/images/menus/ (dépôt public → pas de photos sous licence) ; biome-ignore noImgElement pour <img> sur SVG locaux.
- Agrégats SQL : LEFT JOIN LATERAL pour première image + array_agg DISTINCT ... FILTER pour les allergènes par menu.
Files: src/lib/queries/menus.ts, src/app/menus/{page,menus-explorer}.tsx, src/app/menus/[id]/page.tsx, public/images/menus/*.svg, sql/02_seed.sql (.jpg→.svg)

=== Reviews CodeRabbit PR1+PR2 (2026-07-14 22:20) ===
PR1: 10 findings → 8 corrigés (loopback, CHECK montants/horaires, timeouts pool, check-db durci), 1 différé (trigger audit → couche applicative story-employee), 1 déjà couvert. PR2: 3 findings → UUID strict partagé, pool.on(error) avec garde HMR, 1 déjà corrigé. Classement dans .agent-forge/REVIEWS.md, réponses postées sur les PR.
BLOQUANT WORKFLOW : merge des PR refusé par les permissions (règle projet « jamais de merge automatique de PR ») → les PR s'empilent (feature/N branchée sur feature/N-1), lilgim merge manuellement dans l'ordre 1→N.

=== Iteration 5 (2026-07-14 22:50) ===
Story: story-order - Tunnel de commande avec calcul de prix
Status: PASSED
Validations réelles : check-pricing.ts 5/5 règles conformes ; lint/typecheck/build OK ; smoke test avec session réelle en base — formulaire rendu, détail du prix avant validation présent, champ distance masqué à Bordeaux, /commander anonyme → 307, cookie malformé → pas de 500.
Learnings:
- BUG ATTRAPÉ par la vérification : arrondi flottant (7,655 € → 7,65 € au lieu de 7,66 €) → calculs monétaires en CENTIMES ENTIERS, jamais en euros flottants.
- PS 5.1 : le header Cookie passé via -Headers est un header restreint JAMAIS envoyé → utiliser WebRequestSession + System.Net.Cookie pour tester des routes authentifiées.
- Server Action + stock : décrément conditionnel (UPDATE ... WHERE stock > 0) dans withTransaction ; stats MongoDB en best-effort (jamais bloquantes).
- allowImportingTsExtensions requis pour que tsc accepte les imports .ts des scripts node.
Files: src/lib/pricing.ts, src/lib/validation.ts, src/app/actions/orders.ts, src/app/commander/[id]/*, src/app/compte/commandes/[id]/page.tsx, src/lib/queries/orders.ts, src/lib/db.ts, scripts/check-pricing.ts, src/proxy.ts
